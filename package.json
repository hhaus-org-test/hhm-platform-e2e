{
  "name": "@hhaus-org-test/hhm-platform-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Independent exact-SHA acceptance tests for HHM platform foundations",
  "engines": {
    "node": ">=22.22.1"
  },
  "scripts": {
    "test:contracts": "node --test tests/source-contract.test.mjs",
    "test:pinned": "node scripts/run-pinned-acceptance.mjs",
    "test:interfaces": "node scripts/run-interfaces-acceptance.mjs",
    "test": "npm run test:contracts && npm run test:pinned && npm run test:interfaces"
  }
}
